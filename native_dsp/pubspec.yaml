name: sonik_dsp
description: High-performance C/C++ DSP acceleration and transients engine for 3WM SONIK.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  code_assets: ^0.1.0
  hooks: ^0.1.0
  logging: ^1.2.0
  native_toolchain_c: ^0.5.0
