import 'package:hooks/hooks.dart';

void main(List<String> args) async {
  await link(args, (config, output) async {
    // Tree shaking and dead-code elimination link hook for Sonik Native DSP
  });
}
